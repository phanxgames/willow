# Demos

## Coming Soon

Live WASM demos are planned so you can try Willow directly in your browser — no install required.

In the meantime, you can run the [examples](?page=examples) locally:

```bash
git clone https://github.com/phanxgames/willow.git
cd willow
go run ./examples/shapes
```
